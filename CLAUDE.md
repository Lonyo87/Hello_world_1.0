# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

A static HTML project — no build tools, no dependencies, no server required.

## Running the Project

Open `index.html` directly in a browser. No build or install steps needed.

## Architecture

Single-file frontend: `index.html` contains all HTML structure, CSS styles, and JavaScript inline. There are no external dependencies, frameworks, or assets.
